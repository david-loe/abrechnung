name: Setup Production Environment
description: Copy environment defaults for production image tests without source mounts

runs:
  using: composite
  steps:
    - name: Prepare production environment
      shell: bash
      run: |
        cp .env.example .env
        sed -i 's/^NODE_ENV=development$/NODE_ENV=production/' .env
